*root kasutajana:
installi - apt-get install git
*tavakasutajana:
esmakordselt

a) seadista - maara kasutajanimi. email.editor
git config --global user.name "Ees Perenimi"
git config --global user.email kasutajakonto
git config --global core.editor nano

b) loo kataloog, kus hakkad skriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita laokaalne repositoorium git susteemi jaoks antud kaustas
git init

remote seadistamine:

e) git remote add nimi github:address
git remote add skriptimine git@github.com:kasutajanimi/reponimi.git

f) git remote kontroll
git remote -v

põhikasutamine:

g) pane fail jagamisele
git add failinimi
